import './styles.scss';
const Showcase = ({
    className,
    showcase,
}: {
    className: string,
    showcase: any
}) => {
    return (
        <section className={className}>
            
        </section>
    );
}
export default Showcase;